PORT=3000
DATA_DIR=./data
# Generate three independent values: openssl rand -hex 32
JWT_SECRET=
REFRESH_TOKEN_SECRET=
DB_ENCRYPTION_KEY=
# HTTPS SMS delivery adapter: POST {phone, token, expiresInMinutes}; bearer secret
RECOVERY_WEBHOOK_URL=
RECOVERY_WEBHOOK_SECRET=
# Obtain credentials and configure the callback through Safaricom Daraja.
DARAJA_ENV=sandbox
DARAJA_CONSUMER_KEY=
DARAJA_CONSUMER_SECRET=
DARAJA_SHORTCODE=
DARAJA_PASSKEY=
DARAJA_CALLBACK_URL=
